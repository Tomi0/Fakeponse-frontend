services:
  fakeponse-frontend:
    image: fakeponse-frontend:latest
    container_name: fakeponse-frontend
    ports:
      - "8000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
